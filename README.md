# Press

A personal health/training operating system — see `PRODUCT.md` for what it's for and `ARCHITECTURE.md` for how it's built.

```
npm install
npm run build   # bundles src/app.jsx -> public/app.js
npm test        # runs the backend test suite (node --test)
```
